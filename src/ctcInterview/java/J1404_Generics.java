package ctcInterview.java;

/**
 * Explain the difference between templates in C++ and generic in Java
 * 
 * @author minhhoang
 *
 */
public class J1404_Generics {

	/**
	 * Generic in Java are really just "syntactic sugar" --> 2 same type of
	 * generic do not share static var. C++ does.
	 * 
	 * - C++ can use primitive types. 
	 * 
	 * - Java can tell a generic to subclass
	 * something 
	 * 
	 * - All instances of MyClass, regardless of their type params,
	 * are the same type (Java).
	 * 
	 */

}
