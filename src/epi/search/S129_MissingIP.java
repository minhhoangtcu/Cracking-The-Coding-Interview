package epi.search;

/**
 * Suppose you were given a file containing roughly one billion IP addresses, each of which is a
 * 32-bit quantity. How would you programmatically find an IP address that is not in the file?
 * Assume you have unlimited drive space but only a few megabytes of RAM at your disposal.
 * 
 * @author minhhoang
 *
 */
public class S129_MissingIP {
  /*
   * Option 1: Sort using disk space (extremely slow).
   * Option 2: Find max or min?
   * Option 3: Keep all inside a Map or an array.
   * Option 4: Counting bit by bit.
   */
}
